# Nginx proxy to ISPManager with custom DNS and load balancer

Install latest ansible version
```
pip install ansible
```


Install requirements

```
ansible-galaxy collection install -r requirements.yml
```
